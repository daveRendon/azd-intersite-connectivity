# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: tdd-azd-intersite-connectivity
metadata:
  template: tdd-azd-intersite-connectivity@1.0.0
infra:
   provider: "bicep"
services:
  web:
    project: ./
    language: py
    host: appservice
